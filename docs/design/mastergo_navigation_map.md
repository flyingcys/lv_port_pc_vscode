# MasterGo 页面导航关系整理

## 数据来源

- `fileId=131850344368735`
- 检查页面：
  - 首页：`layerId=160:3454`
  - 场景页：`layerId=160:3601`
  - 设置页：`layerId=162:05153`

## 逐页 interactive 检查结果

| 页面 | layerId | 检查结果 |
|------|---------|----------|
| 首页 | `160:3454` | 当前 DSL 输出中未发现 `interactive` 导航元数据 |
| 场景页 | `160:3601` | 当前 DSL 输出中未发现 `interactive` 导航元数据 |
| 设置页 | `162:05153` | 当前 DSL 输出中未发现 `interactive` 导航元数据 |

## DSL 原生导航关系表

当前 Task 1 提取范围内，未发现可直接写成“页面 A -> 页面 B”的 MasterGo `interactive` 导航关系。

| 来源页面 | 目标页面 | 来源 |
|----------|----------|------|
| 无 | 无 | 当前 DSL 未发现 `interactive` 元数据 |

## 基于需求文档的 LVGL 手工导航建议

以下内容不是 DSL 原生导航事实，而是依据 `docs/requirements/ai_voice_screen_switch_lvgl_requirements.md` 建议在 LVGL 中手工实现的交互关系：

| 来源 | 目标 | 触发方式 | 依据 |
|------|------|----------|------|
| 首页 | 场景页 | 左右滑动 | 需求文档 `UI/交互 -> 页面结构与导航` |
| 场景页 | 首页 | 左右滑动 | 需求文档 `UI/交互 -> 页面结构与导航` |
| 首页/场景页 | 设置页 | 顶部下拉 | 需求文档 `UI/交互 -> 页面结构与导航` |
| 任意主页面 | 语音弹窗 | 语音会话触发 | 需求文档 `UI/交互 -> 语音交互相关 UI 状态` |
| 设置页 | 关于二级内容（推测） | 点击右箭头或对应项 | 需求文档 `设置页` 与 DSL 视觉结构共同提示，但仍需产品/原型进一步确认 |

## 结论

- Task 1 范围内，MasterGo DSL 没有提供可直接复用的页面跳转元数据。
- 因此后续 LVGL 页面切换、顶部下拉设置、语音弹窗等逻辑，都应由应用路由层手工实现。
- “关于”是否进入单独二级页，目前只能视为基于需求文档和视觉结构的实现推测，不应当作已被 DSL 证实的导航事实。
